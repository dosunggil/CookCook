<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  

 <mapper namespace="com.ahn.recipe.dao.UserDao">
 
 	<select id="selectAll" resultType="UserVO">
 		SELECT * FROM cookcook
 	</select>
 
 	<select id="findById" resultType="UserVO">
 		SELECT * FROM cookcook WHERE username = #{username}
 	</select>
 	
 	<insert id="insert" parameterType="UserVO">
 		INSERT INTO cookcook
	 		(
		 		username, 
		 		password, 
		 		name, 
		 		nickname,
		 		email
	 		) VALUES (
	 			#{username}, 
	 			#{password}, 
		 		#{name}, 
		 		#{nickname}, 
		 		#{email}
	 		)
 	</insert>
 	
 	<update id="update" parameterType="UserVO">
 		UPDATE cookcook SET
 				password = #{password}, 
		 		email = #{email}, 
		 		name = #{name},
		 		nickname = #{nickname} 
 		WHERE username = #{username}
 	</update>
 	<delete id="delete">
 		DELETE FROM cookcook WHERE username = #{username}
 	</delete>
 </mapper>
 
 